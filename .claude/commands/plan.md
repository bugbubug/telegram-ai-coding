请为以下任务创建一个分阶段实施计划：

$ARGUMENTS

计划要求：
1. 分解为可独立提交的小任务（每个任务 < 100 行改动）
2. 标注每个任务涉及的模块和文件
3. 标注任务间的依赖关系
4. 每个任务包含验证步骤（测试/类型检查）
5. 预估每个任务的复杂度（低/中/高）
6. 若涉及用户可见行为、Bot 命令、Agent 协作规则或插件接口，明确列出要同步更新的文档文件

输出一个带复选框的 Markdown 清单。
